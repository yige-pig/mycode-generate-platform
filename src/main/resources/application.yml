spring:
  profiles:
    active: local
  application:
    name: mycode-generate-platform-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/code_generate_platform
    username: root
    password: lly061
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.yige.mycodegenerateplatform.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

